{
  "name": "polyglotai",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:desktop": "pnpm --filter @polyglotai/desktop-tauri tauri dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
