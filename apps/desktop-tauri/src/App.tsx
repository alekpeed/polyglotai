import "./App.css";

/**
 * Scaffold placeholder. Real screens (onboarding, dashboard, lesson, review, tutor,
 * conversation, pronunciation, vocab/grammar/slang libraries, pack manager, settings) are
 * built out per the implementation plan's build sequence, not all at once.
 */
function App() {
  return (
    <main className="container">
      <h1>PolyglotAI</h1>
      <p>Scaffold running.</p>
    </main>
  );
}

export default App;
