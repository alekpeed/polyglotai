# Web (placeholder, optional/future)

Not built in the MVP. An optional future web app, per spec §2's later platform targets,
would reuse the same `packages/*` as the desktop app and Android, behind a browser-hosted
shell with cloud sync instead of local SQLite.
