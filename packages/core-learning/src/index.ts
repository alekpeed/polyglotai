/**
 * @polyglotai/core-learning — pure TS, no React, no Tauri (plan §2). Houses the DB
 * repositories, profile/vocabulary/grammar/slang/dialogue services, the thin learning
 * engine, feature flags, migrations, and the event bus.
 *
 * This is a scaffold placeholder: modules are built out step-by-step per the plan's build
 * sequence (Milestone A step 5 onward), not all at once.
 */
export const CORE_LEARNING_PACKAGE_NAME = "@polyglotai/core-learning";
